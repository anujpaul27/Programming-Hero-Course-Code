// Assign the value 5 to x
let x = 5;
console.log(x);

const an = 10;
const am = 20;
const adding = an + am;
const difference = am - an;
const multiple = an * am;
const devide = am / an; // This a back word 
const pow = 3**2  // output should be 9 

console.log('Power ', pow);

{let an = 100 ;
    an = 200;
    console.log(an );
}